import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['app.netlify.com', 'vercel.com', 'arifh97.vercel.app', 'arifh97.netlify.app']
  }
};

export default nextConfig;
